using System.Collections.Generic;

namespace com.onlineobject.objectnet {
    /// <summary>
    /// Represents a mapping of network event message names to their corresponding integer codes.
    /// </summary>
    public class NetworkEventsMap {

        // Dictionary to hold the mapping between message names and their integer codes.
        private Dictionary<string, int> MessageNameMap = new Dictionary<string, int>();

    }
}